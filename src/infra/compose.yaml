services:
  database:
    container_name: "postgres-dev"
    image: "postgres:16"
    restart: unless-stopped
    env_file:
      - ../../.env.development
    ports:
      - "5432:5432"

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
